apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pipeline-application.fullname" . }}-test-connection"
  labels:
    {{- include "pipeline-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pipeline-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
